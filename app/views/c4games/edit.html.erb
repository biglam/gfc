<h1>Connect Four</h1>

<h2>Current Player - <%= @game.p1.email if @game.turn==1 %><%= @game.p2.email if @game.turn==2 %></h2>
<p>Yes, I know there's gems, but I'm a masochist...</p>


<%= form_for @game do |f| %>
<table class="c4board">
  <% 7.times do |i| %>
  <th class="c4cell">
  <%= radio_button_tag(:column, i) %></th>
  <% end %>

  <% 6.times do |i| %>
    <tr>
      <% @game.c4board.send("r#{i+1}").split(//).each do |cell| %>
      <td class="c4cell"><%= cell %></td>
      <% end %>
    </tr>
  <% end %>
</table>
<%= f.submit 'Make Move' %>
<% end %>